# skill-creator iteration workspaces (run outputs, grading, viewer feedback)
skills/*-workspace/
.DS_Store

# eval definitions and fixtures stay local (public repo ships skills only)
skills/*/evals/

# QA methodology stays local (public repo ships skills only)
_shared/test-loop.md
